kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: it-tools
  namespace: it-tools
spec:
  path: /
  to:
    kind: Service
    name: it-tools
  port:
    targetPort: it-tools
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
